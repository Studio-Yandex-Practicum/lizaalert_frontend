// import type { AppState } from '../types';

// export const selectIsRegisterLoading = (state: AppState) =>
//   state.register.isLoading;
// export const selectRegisterError = (state: AppState) => state.register.error;
// export const selectIsRegister = (state: AppState) => state.register.isRegister;
