pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{css,scss}'
      run: npm run prettier --ignore-unknown --write {staged_files} && git add {staged_files}
    lint:
      glob: '*.{ts,tsx}'
      run: npm run eslint --fix {staged_files} && git add {staged_files}
